# Global prettier configuration

overrides:
  # Markdown files
  - files: "*.md"
    options:
      printWidth: 80
      # preserve, not never: "never" joins paragraphs and undoes sentence-per-line authoring
      proseWrap: "preserve"
