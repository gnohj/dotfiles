# Auto-generated tmux colors from active colorscheme
# Generated at: Fri Jan 16 20:54:25 EST 2026

# Status bar colors (transparent background)
set -g status-style "bg=default,fg=#e9e9e2"
set -g status-left-style "fg=#a3b8c6,bg=default"
set -g status-right-style "fg=#9fb7a4,bg=default"

# Use status-format for complete control - everything centered as one unit
set -g status-format[0] "#[align=centre]#(/Users/gnohj/.config/tmux/generate-status-line.sh '#{pane_id}')"

# Empty status-left and status-right since we're using status-format
set -g status-left ""
set -g status-right ""

# Empty window status formats to prevent duplication
set -g window-status-current-format ''
set -g window-status-format ''

# Pane border colors
set -g pane-border-style "fg=#a7cfaf"
set -g pane-active-border-style "fg=#a7cfaf"

# Message colors (display-message)
set -g message-style "bg=default,fg=#a3b8c6"
set -g message-command-style "bg=default,fg=#a3b8c6"

# Copy mode colors (selection highlight)
set -g mode-style "bg=#536571,fg=#b7ce97"
