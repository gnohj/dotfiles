#!/usr/bin/env bash
set -euo pipefail

args=("$@")
if [ "${AGENT_BROWSER_HEADLESS:-0}" = 1 ]; then
  args+=(--headless --chrome-arg=--mute-audio)
fi
exec npx -y chrome-devtools-mcp@1.8.0 "${args[@]}"
