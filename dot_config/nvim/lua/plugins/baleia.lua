-- https://github.com/m00qek/baleia.nvim - ANSI→highlights in a normal buffer; the tmux prefix+e scrollback viewer requires it directly from this lazy path (lazy=true still clones it on sync).

return {
  "m00qek/baleia.nvim",
  lazy = true,
}
