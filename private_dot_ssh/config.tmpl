Host github.com
  AddKeysToAgent yes
{{- if eq .chezmoi.os "darwin" }}
  # macOS: sign with the keychain-held identity key. On the VPS this block is
  # omitted — no private key on the box, git rides the forwarded agent instead.
  IgnoreUnknown UseKeychain
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519
{{- end }}

# dev-box VPS (uncomment + set HostName once the box exists). Agent
# forwarding lets `git push` from the box sign with the key held in the Mac's
# agent — the private key never lands on the server.
# Host dev-box
#   HostName dev-box.<tailnet>.ts.net
#   User gnohj
#   ForwardAgent yes
#   IdentityFile ~/.ssh/id_ed25519