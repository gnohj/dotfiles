# Inferno-monorepo overlay — merged on top of config.yml when lg() detects
# git remote origin contains "inferno-monorepo".
#
# P now performs a normal direct push to origin (lazygit's built-in pushFiles).
# The no-mistakes gate lives on N (see config.yml): `git push no-mistakes <branch>`.
# This overlay is intentionally a no-op; kept because lg() still references it.
