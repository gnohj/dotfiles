# Auto-generated tmux colors from active colorscheme
# Generated at: Sat Aug 30 12:30:44 CDT 2025

# Status bar colors (transparent background)
set -g status-style "bg=default,fg=#e9e9e2"
set -g status-left-style "fg=#a3b8c6,bg=default"
set -g status-right-style "fg=#9fb7a4,bg=default"

# Status left: Session name + repo name + gitmux (reactive to current pane)
set -g status-left "#[fg=#a3b8c6,nobold]#S#[fg=#e9e9e2,nobold]#(PANE_ID='#{pane_id}'; PANE_NVIM_CWD=\$(tmux show-environment -t \"\$PANE_ID\" \"NVIM_CWD_\$PANE_ID\" 2>/dev/null | cut -d= -f2); DIR=\"\${PANE_NVIM_CWD:-#{pane_current_path}}\"; cd \"\$DIR\" 2>/dev/null && REPO_NAME=\$(basename \"\$(git rev-parse --show-toplevel 2>/dev/null)\" 2>/dev/null); OUTPUT=\$(gitmux -cfg \$HOME/.config/gitmux/gitmux.yml); [ -n \"\$REPO_NAME\" ] && echo \"#[fg=#dc988e]\$REPO_NAME#[fg=#e9e9e2]\$OUTPUT\" || echo \"\$OUTPUT\") "
set -g status-left-length 100

# Status right: Empty (minimalist)
set -g status-right ""

# Window status colors
set -g window-status-current-format '#[fg=#a7cfaf]*#[fg=#a3b8c6]#W'
set -g window-status-format ' #[fg=#505e62]#W'

# Pane border colors
set -g pane-border-style "fg=#536571"
set -g pane-active-border-style "fg=#a3b8c6"

# Message colors (display-message)
set -g message-style "bg=default,fg=#a3b8c6"
set -g message-command-style "bg=default,fg=#a3b8c6"

# Copy mode colors (selection highlight)
set -g mode-style "bg=#a3b8c6,fg=#0f1419"
