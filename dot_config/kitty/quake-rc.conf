# Layered onto kitty.conf via `kitty_conf`, not kitty_override — the empty-password form
# cannot survive `name=value`. Empty password = allowed with none sent; rc-auth.py judges.
allow_remote_control password
remote_control_password "" rc-auth.py
